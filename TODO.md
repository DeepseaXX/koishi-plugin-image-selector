# 多层合并转发存图 TODO

执行方案见 [IMPLEMENT_PLAN.md](./IMPLEMENT_PLAN.md)。

- [x] 核实源码、仓库状态并写入执行计划。
- [x] 统一解析 Koishi 元素、OneBot 消息段、CQ 字符串与消息包装。
- [x] 完成嵌套记录读取、顺序遍历、请求缓存和重复/循环引用处理。
- [x] 加入层数、节点数、请求超时限制及局部失败提示。
- [x] 接入引用、直接参数和交互式存图，保留现有保存规则。
- [x] 同步 README、入口说明、命令帮助与插件桌面描述。
- [x] 通过离线回归和插件构建，检查最终改动。

验证结果：目标构建通过；32 个离线用例通过（含实际存图 action 的文件输出验证）。开发任务已完成。

## 用户手测（真实 QQ/Koishi 环境）

- [ ] 回复含三层以上嵌套、内外层都有图片/视频的合并记录，执行 `存图 关键词`，核对文件数量和顺序。
- [ ] 按交互提示发送同一记录，确认同样能够保存。
- [ ] 确认部分记录过期或读取失败时，其他图片仍能保存并收到不完整提示。
